default: &default
  adapter: mysql2
  encoding: utf8
  pool: 5

development:
  <<: *default
  host: localhost
  database: reversal
  username: reversal
  password: pw

test:
  <<: *default
  host: localhost
  database: test

production:
  <<: *default
  host:     <%= ENV['RAILS_DATABASE_HOST'] %>
  database: reversal
  username: <%= ENV['RAILS_DATABASE_USER'] %>
  password: <%= ENV['RAILS_DATABASE_PASSWORD'] %>

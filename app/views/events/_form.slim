.m-t-1
  - if event.errors.any?
      - event.errors.full_messages.each do |message|
        .alert.alert-warning role='alert' = message

  = bootstrap_form_for(event, label_errors: true) do |form|
    .row
      .col-xs-6
        label for='event_title' タイトル
        .input-group
          .input-group-addon
            i.fa.fa-pencil
          = form.text_field :title, hide_label: true
    .row
      .col-xs-4
        label for='event_datetime' 日時
        .input-group
          .input-group-addon
            i.fa.fa-calendar
          = form.text_field :datetime, hide_label: true, class: 'datetimepicker'
    = form.text_area :description, class: 'ckeditor'
    = form.submit '登録', class: 'btn btn-primary btn-lg'

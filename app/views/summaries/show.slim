.uk-panel.uk-panel-box.uk-margin-top
  = render 'shared/item_header', item: @summary
  hr
  = render 'item', summary: @summary
  = render 'shared/item_authority', current_user: @current_user, item: @summary, edit_path: edit_summary_path, delete_path: page_path(@summary)

- content_for :title do
  = @summary.title
